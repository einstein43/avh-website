/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind CSS is no longer used
  // All styling is now handled by pure CSS variables in app/globals.css
  // This file is kept for backwards compatibility only
};
